// tRPC has been removed. This stub prevents build errors in dead-code
// controllers (RecordController, PlaybackController, SessionBrowser)
// that will be cleaned up in a future PR.
export const trpc = null as never
