/** Metric: full gauntlet breakdown (one per evaluate() call). */
export const METRIC_GAUNTLET_BREAKDOWN = 'gauntlet.breakdown'

/** Metric: per-episode fitness breakdown (one per episode). */
export const METRIC_EPISODE_FITNESS = 'episode.fitness'
